# 政途人生 开发路线图

> 本文档记录当前可验证的实现基线和后续开发顺序。
> 最后校准：2026-07-21，基于 `main` 合并基础工程 PR #88 后的状态。
> 当前版本：0.1.0-alpha.1

## 已完成：项目工程与纵向切片基础

- CI 质量门禁和项目规范（PR #47）
- 行政线 L1-L3 纵向切片（10 个职位配置）
- 五步角色建档
- 行动槽位系统（6 槽位 + 分类 + 冷却）
- 时间推进与到期结算
- KPI 与五维年度考核
- 当前晋升流程（7 阶段）
- localStorage 实时存档
- 启动页继续/建档入口

## 已完成：基础工程重构

PR #88 完成以下基础工程整理：

- 严格存档解码器（Zod `.strict()` 全层级 + 跨字段一致性校验）
- `SaveEnvelope` 存档封装 + 不兼容存档安全备份
- 统一时间轴引擎（修复结算顺序和跨年 13 月事件）
- 行动实例级运行时快照（消除玩家级临时倍率）
- Store Reducer 分域拆分（game-store.ts 从 1064 行精简到约 290 行）
- 条件持久化（仅实际状态变化时写档）
- 启动页存档错误分类提示
- 相关单元测试和集成测试

## 当前：Phase 1.5 项目基线校准

- 正式版本体系（`0.1.0-alpha.1`）
- 软件版本、Schema 版本、内容版本分离
- README、架构文档、路线图校准
- 版本管理规范入库（`docs/VERSIONING.md`）
- 职业与治理改版指导入库（`docs/CAREER_REDESIGN_GUIDE.md`）
- CHANGELOG 建立
- 历史设计文档标记
- Issue 清理和 Phase 2 追踪创建

## 下一阶段：Phase 2 职业领域模型

目标：建立支撑完整职业生涯的数据模型基础。

1. 定义机构层级（乡镇→县区→地级市→省级→中央）
2. 定义岗位领域（替代固定职业线）
3. 定义领导职务层次（乡科级副职→国家级正职）
4. 建立公务员职级数据模型（二级科员→巡视员）
5. 建立当前任职状态（具体职位 + 层次 + 职级 + 机构层级）
6. 建立职业履历记录
7. 保留 `contentTier` 兼容层（减少配置一次性重写）
8. 升级存档 Schema（schemaVersion 1 → 2）
9. 实现职级晋升最小闭环
10. 实现岗位机会驱动的领导职务选拔入口

完成标准：

- 领导职务和公务员职级可以独立变化
- 玩家不能随时主动触发领导岗位晋升
- 岗位领域可以通过交流改变
- 旧行政低等级配置通过适配层继续可用

## 后续阶段

按依赖顺序规划（Phase 编号仅用于项目管理，不代表软件版本）：

### Phase 3：基层纵向切片重做

- 试用期、科员、主任科员、乡科级副职、乡科级正职
- 验证职级和领导职务双通道是否有趣
- 科员阶段管理具体任务而非多个部门

### Phase 4：NPC 和岗位机会

- NPC 干部生命周期
- 岗位空缺产生
- 交流和换届
- 职级晋升
- 领导岗位选拔
- 相对竞争

### Phase 5：县市和省部级内容

- 县处级、厅局级、省部级
- 地方与中央交流
- 全国性重大任务

### Phase 6：国家级副职原型

- 中央领导协作
- 五年政治周期
- 国家级正职候选池
- NPC 竞争

### Phase 7：国家级正职终局

- 国务院总理、全国人大常委会委员长、全国政协主席
- 国家主席、中共中央总书记、中央军委主席
- 三职兼任最高传奇终局

### Phase 8：扩展治理系统

- 关系网络与上级互动
- 秘书、公文和舆情
- 风险、调查与廉政
- 个人生活、退休和历史评价

### Phase 9：云存档

- Supabase Auth
- 启动时远程加载
- 本地/远程仲裁
- 离线队列和同步状态

## 质量门禁

```bash
pnpm run ci    # format + lint + typecheck + test + validate:config + build
```

覆盖率门槛：Engine ≥ 90%、Config ≥ 80%、Store ≥ 70%。

## 开发约定

### 分支与 PR

从 main 创建 `type/description` 分支，通过质量门禁后创建 PR，CI 和审查通过后 Squash Merge。禁止直接推送 main。

### 版本管理

详见 [docs/VERSIONING.md](docs/VERSIONING.md)。Commit 和 PR 标题不使用代际式"v4"命名。

### 变更检查清单

- 新增 Engine 函数：添加同域单元测试，从 `src/engine/index.ts` 导出
- 新增 Store action：先更新 `GameAction`，再在 `reduceGameState()` 增加分支和测试
- 修改 `PlayerSave`：同步默认状态、存档校验和相关测试
- 修改 JSON 结构：同步 TypeScript 类型、zod schema、loader 和配置校验
- 修改 JSON 数值：运行 `pnpm validate:config`

## 长期设计方向

完整产品方向参阅 [docs/CAREER_REDESIGN_GUIDE.md](docs/CAREER_REDESIGN_GUIDE.md)。

历史设计参考：[zhengtu-v3-design.md](zhengtu-v3-design.md)（正式版本体系建立前的文档，其中"v3.0"不代表当前软件版本）。
# 政途人生 v3.0 开发路线图

> 本文档记录当前可验证的实现基线和后续开发顺序。
> 最后校准：2026-07-18，基于 `main` 合并 PR #47 后的状态。

## 1. 产品基线

### 1.1 当前目标

当前阶段以**本地单机优先、行政线 L1-L3 可完整体验**为目标。先打通并打磨一个纵向切片，再扩展行政高等级、其他职业线和云端能力。

当前范围包括：

- 五步角色建档。
- 行政线 L1-L3（科员、副科、正科）。
- Dashboard 中的部门行动排期、时间推进、KPI、年度考核和晋升。
- localStorage 实时自动存档。

不属于当前完成范围：

- 行政线 L4-L11。
- 党务、纪检、群团职业线的可玩内容。
- Supabase Auth、启动时远程加载和完整云存档流程。
- 关系、上级互动、风险、个人生活、秘书和历史评价等扩展系统。

### 1.2 当前配置

| 配置     | 当前实现                                      |
| -------- | --------------------------------------------- |
| 职业线   | 4 个配置文件；仅行政线包含职位                |
| 行政等级 | L1-L3                                         |
| 行政职位 | 10 个配置职位；职位选择逻辑固定取每级首个职位 |
| 部门模板 | 核心与扩展模板合并后共 19 个                  |
| KPI 模板 | 32 个                                         |
| 行动模板 | 38 个                                         |

每次晋升前必须先完成所有在途行动。晋升成功后会立即归档旧任期，并初始化新职位预算、部门和当前任期考核状态；玩家确认结果后可继续任职。L3 没有下一等级配置，因此无法启动通往 L4 的晋升。

### 1.3 当前页面

实际注册路由以 `src/app.tsx` 为准：

| 路由         | 页面           | 状态 |
| ------------ | -------------- | ---- |
| `/`          | Splash         | 可用 |
| `/character` | 五步建档       | 可用 |
| `/dashboard` | 综合 Dashboard | 可用 |

部门行动、KPI 和晋升交互已经合并到 Dashboard。`src/pages/career/` 下的独立页面目前不是可达路由，不应作为已完成页面宣传。

## 2. 当前游戏循环

```text
Splash → 继续本地存档 → Dashboard
       ↘ 开始新游戏 → 五步建档 → NEW_GAME → Dashboard
  ↓
选择部门行动 → START_ACTION
  ↓
校验分类、预算、重复行动、冷却和空位
  ↓
  占用槽位并立即扣除预算（备用槽额外处罚）
  ↓
推进 1 / 7 / 30 天 → ADVANCE_TIME
  ↓
到期行动结算 KPI 与玩家属性效果，并释放槽位
  ↓
月度预算结算 / 年度 KPI 考核
  ↓
满足条件后进入一次晋升流程
  ↓
成功后初始化新职位 → 确认结果 → 继续积累考核与任职年限
```

### 2.1 槽位模型

| 槽位等级 | 数量 | 用途                         |
| -------- | ---- | ---------------------------- |
| 主要     | 3    | 核心工作                     |
| 次要     | 2    | 常规工作                     |
| 备用     | 1    | 加班槽位，附带健康和消沉惩罚 |

槽位数量固定，不随推进粒度变化。行动通过 `durationDays` 决定完成日期，通过 `category`（重大/次要/日常）决定可用槽位等级和冷却规则。

行动分类及规则：

| 分类 | 可用槽位       | 冷却天数 | 同部门并行 |
| ---- | -------------- | -------- | ---------- |
| 重大 | 仅主要         | 14 天    | 禁止       |
| 次要 | 主要/次要/备用 | 7 天     | 禁止       |
| 日常 | 主要/次要/备用 | 无冷却   | 允许       |

玩家在 UI 中选择槽位等级，系统取该等级第一个空位。冷却从名义完成日（startedAtDay + durationDays）起算，按部门实例记录绝对截止日。备用槽位按 `constants.json` 中的 `reservePenalty` 扣减健康并增加消沉值。

### 2.2 晋升流程

晋升状态依次经历：

1. 民主推荐。
2. 组织考察。
3. 联审。
4. 常委会票决。
5. 公示。
6. 任命。
7. 试用期。

晋升期间会锁定普通行动和时间推进，仅允许处理当前晋升阶段要求的交互。

## 3. 技术基线

### 3.1 技术栈

```text
框架：SolidJS + TypeScript + Vite
状态：createStore + produce
路由：自建 hash router
测试：Vitest
配置：JSON + ConfigLoader + zod 完整性校验
存储：localStorage；Supabase 远程存档代码尚未贯通产品流程
部署：GitHub Pages
```

### 3.2 分层架构

```text
UI（Solid 页面/组件）
  → Store（createStore + dispatch）
    → Engine（纯函数）
      → Config（JSON + loader）
```

关键约束：

- Engine 不访问 DOM、全局状态或 Store。
- 所有游戏状态变更通过 `dispatch(action)` 和 `reduceGameState()`。
- Store 逻辑测试使用 `createTestStore()` 隔离；持久化集成测试可以使用模块级 `dispatch()`。
- 玩家数值变更通过 `applyPlayerAttr()` 校验并钳位。
- 游戏数值优先放在 JSON 配置中。
- Solid 列表渲染使用 `<For>`，不使用 JSX 中的 `Array.map()`。

### 3.3 存档现状

- 模块级 `dispatch()` 每次操作后写入 localStorage。
- 所有 action 仅写入 localStorage，运行时远程同步暂时停用。
- 应用启动读取 localStorage，并在 Splash 提供继续游戏与重新建档入口。
- `fetchRemoteSave()` 和 `selectNewer()` 已实现，但尚未接入启动流程。
- 登录路由暂不注册，当前不提供用户级云存档。

## 4. 质量门禁

本地提交 PR 前按以下顺序执行质量门禁：

```bash
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm validate:config
pnpm build
```

可使用 `pnpm run ci` 依次运行完整门禁；GitHub Actions 中的检查以独立 job 并行执行。不要使用裸 `pnpm ci`，后者是 pnpm 自带命令。

当前测试基线以 `pnpm run ci` 的实际输出为准，不把固定用例数量作为长期规模指标。

覆盖率门槛：

- Engine：行覆盖率不低于 90%。
- Config：行覆盖率不低于 80%。
- Store：行覆盖率不低于 70%。

## 5. 开发阶段

### 阶段 0：工作流与项目基线

状态：已完成（PR #47）。

已完成：

- 工作流最小权限和不可变 Action SHA。
- `/oc`、`/opencode` 评论命令的触发身份限制。
- Pages 仅部署通过 `main` CI 的精确提交。
- README 与路线图按当前实现校准。
- `ARCHITECTURE.md` 和 `CONTRIBUTING.md` 已按固定槽位、实时本地存档和当前模块校准。

### 阶段 1：完成行政 L1-L3 纵向切片

目标：从建档到 L3 封顶的流程清晰、稳定、可恢复。

已完成：

- 修正首次晋升完成后的状态重置和继续晋升入口。
- 隔离新旧职位预算、考核和部门状态。
- 在途行动完成前禁止启动晋升，并为 L3 提供封顶反馈。
- 实现行动分类体系（重大/次要/日常），玩家手动选择槽位等级，冷却和备用槽处罚可达。
- 暂停登录和运行时远程同步，启动页可继续有效本地存档或重新建档。

待完成：

1. 完善 Dashboard 摘要，并提供清晰的 KPI、晋升详情体验。
2. 清理或重新接入不可达的职业独立页面。

完成标准：

- 新玩家可从建档连续游玩到 L3。
- 刷新页面后可明确继续已有游戏。
- 行动排期、预算、到期结算、年度考核和晋升均有可理解反馈。
- L3 封顶状态有明确说明。

### 阶段 2：核心流程测试与缺陷清理

目标：在扩充内容前稳定现有行为。

1. 增加建档 → 行动 → 推进时间 → 考核 → 晋升的 Store/集成测试（Issue #19）。
2. 补齐新槽位系统的预算、重复行动、备用槽惩罚和跨周期结算测试。
3. 处理已知代码规范问题（Issue #13）。
4. 清理未使用依赖和废弃配置（Issue #15）。
5. 将仍在引擎中的晋升数值迁移到配置（Issue #12）。

### 阶段 3：扩展行政线 L4-L11

目标：解除 L3 职业进度封顶。

1. 为 L4-L11 设计职位、部门、KPI、预算与晋升条件。
2. 逐级校验引用完整性和经济数值。
3. 增加高等级阶段内容与结束条件。
4. 避免一次提交全部占位数据，按可验证等级段分批交付。

### 阶段 4：扩展系统

在行政主循环稳定后，依次考虑：

1. 上级互动与人脉网络。
2. 转职与后备干部体系。
3. 秘书、公文和舆情。
4. 风险、调查与廉政系统。
5. 个人生活、退休和历史评价。

### 阶段 5：认证与云存档

目标：在本地体验稳定后提供可恢复的用户级远程存档。

1. 接入 Supabase Auth。
2. 定义用户 ID、匿名存档和登录后存档迁移规则。
3. 启动时并行加载本地和远程存档。
4. 使用 `updatedAt` 仲裁，并处理时钟偏差和冲突。
5. 增加离线、失败重试和用户可见的同步状态。

## 6. 当前问题

以下问题来自当前代码核对，应进入对应阶段处理：

| 问题                     | 影响                     | 计划阶段 |
| ------------------------ | ------------------------ | -------- |
| L3 没有下一等级配置      | 晋升封顶                 | 阶段 3   |
| 核心游戏流程缺少集成测试 | 回归风险较高             | 阶段 2   |
| 备用槽对现有行动不可达   | 三级槽位体验不完整       | 阶段 1   |
| 登录与远程同步暂时停用   | 无真实用户身份与云恢复   | 阶段 5   |
| 启动时不读取远程存档     | 云存档无法恢复           | 阶段 5   |
| 独立 KPI/晋升页面未注册  | 页面结构与遗留文件不一致 | 阶段 1   |
| 事件配置未接入运行时处理 | 配置内容不可玩           | 阶段 4   |

## 7. 开发约定

### 7.1 分支与 PR

```text
1. 从 main 创建 type/description 分支
2. 编写代码和测试
3. 按顺序通过质量门禁
4. 推送分支并创建 PR
5. CI 和 OpenCode Review 通过后 Squash Merge
```

禁止直接推送 `main`。分支使用 `feat/`、`fix/`、`refactor/`、`docs/` 或 `chore/` 前缀。

提交信息与 PR 标题、正文使用中文，保留 Conventional Commits 类型和可选 scope，例如：

```text
feat(store): 完善备用槽行动调度
fix(save): 恢复存档后进入仪表盘
docs: 校准项目开发路线图
```

### 7.2 变更检查清单

- 新增 Engine 函数：添加同域单元测试，并从 `src/engine/index.ts` 导出。
- 新增 Store action：先更新 `GameAction`，再在 `reduceGameState()` 增加分支和测试。
- 修改 `PlayerSave`：同步默认状态、存档校验和相关测试。
- 修改 JSON 结构：同步 TypeScript 类型、zod schema、loader 和配置校验。
- 修改 JSON 数值：运行 `pnpm validate:config`。
- 修改 UI：验证桌面和移动端，并保持 SolidJS 项目约定。

## 8. 长期设计目标

完整设计仍以 `zhengtu-v3-design.md` 为参考，包括：

- 行政、党务、纪检、群团四条职业线。
- L1-L11 职业等级。
- 社交、办公室、风险、个人生活与历史评价系统。
- 更丰富的职位、部门、行动和随机事件。

这些内容是方向，不应在 README 或路线图中表述为当前已实现能力。每个阶段都应以可运行、可测试的纵向结果交付。
